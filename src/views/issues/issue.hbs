<div id="main">
    {{#if viewData}}
        <h1 class="pageHeader">Issue: {{viewData.title}}</h1>
        <h2>Select one of the alternatives below to modify this issue.</h2>
        <!--<img src="{{viewData.avatar}}"/>
        <p>{{viewData.description}}</p>
        <p>{{viewData.iid}}</p>
        <p>{{viewData.status}}</p> -->

        {{#if viewData.status}}

        <a id="issueOpenClose" href="/issues/issue/{{viewData.iid}}/close">Close issue</a>
        {{else}}
        <a id="issueOpenClose" href="/issues/issue/{{viewData.iid}}/reopen">Reopen issue</a>
        {{/if}}
    {{/if}}

</div>